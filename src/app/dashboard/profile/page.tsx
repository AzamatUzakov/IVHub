import ProfileForm from "@/components/ProfileForm";

const profile: React.FC = () => {
    return (
        <div>

            
            <ProfileForm/>
        </div>
    );
}

export default profile;